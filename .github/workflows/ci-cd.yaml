name: ASP.NET Framework CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows-2019

    steps:
      - name: setup-msbuild
        uses: microsoft/setup-msbuild@v2

    
      - name: folder
        run: |
          cd "C:\Program Files (x86)\Microsoft Visual Studio"
          dir

      - name: Build
        run: msbuild Demo_ASPNET/Demo_GitHub/Demo_GitHub.csproj -t:rebuild -verbosity:diag -property:Configuration=Release
        

  
